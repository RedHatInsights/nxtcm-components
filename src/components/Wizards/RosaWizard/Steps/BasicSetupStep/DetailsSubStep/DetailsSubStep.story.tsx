/** Mock data, types, and factories reused by Details substep stories and component tests. */
export * from './DetailsSubStep.fixtures';

/** Mountable Details substep (wrapped form + strings) for Storybook or Playwright without importing `*.stories`. */
export { DetailsSubStepMount as DetailsSubStepStory } from './DetailsSubStep.spec-helpers';
